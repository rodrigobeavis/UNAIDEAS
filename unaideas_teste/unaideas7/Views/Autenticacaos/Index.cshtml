@model IEnumerable<unaideas7.Models.Autenticacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pw)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.area_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.acesso_user)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pw)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.area_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.acesso_user)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_autenticacao }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_autenticacao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_autenticacao })
        </td>
    </tr>
}

</table>
