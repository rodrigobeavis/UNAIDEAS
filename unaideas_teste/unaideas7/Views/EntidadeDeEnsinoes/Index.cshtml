@model IEnumerable<unaideas7.Models.EntidadeDeEnsino>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome_entidade_ensino)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao_entidade_ensino)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome_entidade_ensino)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao_entidade_ensino)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_entidade_ensino }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_entidade_ensino }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_entidade_ensino })
        </td>
    </tr>
}

</table>
